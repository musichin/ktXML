package com.github.musichin.ktxml

import org.fest.assertions.api.Assertions.assertThat
import org.junit.Test
/*
class CommentElementTest {
    @Test fun testNewBuilder() {
        val cdataElement = CommentElement("myComment").newBuilder().build()

        assertThat(cdataElement.comment).isEqualTo("myComment")
    }
}*/